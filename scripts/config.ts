/* 通用配置 */
import * as path from 'path';

export const dir: string = path.join(__dirname, '../packages');

export const packageNames: string[] = [
  'milktea',
  'milktea-cli',
  'server',
  'server-log',
  'util-tools'
];