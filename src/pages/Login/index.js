import ConfigProvider from '../../layouts/ConfigProvider';
import Login from './Login';

export default {
  metaInfo: {
    title: 'Webpack App - login'
  },

  render() {
    return (
      <Login />
    );
  }
};